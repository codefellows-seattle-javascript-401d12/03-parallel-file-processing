'use strict';

const files = require('../index.js');
const expect = require('chai').expect;

describe('File Module', function(){
  describe('#readFileContent', function(){
    it('should gurantee the files are read in the proper order', function(){
        var result = files.readFileContent(null,);

    });
    it('should check for improper paths', function(){

    })
  });
});
